services:
  - type: web
    name: crypto-intel-api
    runtime: docker
    plan: free
    region: frankfurt
    healthCheckPath: /api/status
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
